/tmp
/out-tsc

/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/.pnp
.pnp.js

.vscode/*

# IDE
.idea/

# Build
/dist
/backend/dist
/backend/build
/backend/*.spec.bak
# Locally-built whisper.cpp-Vulkan binary output (backend/scripts/
# build_whisper_cpp_vulkan.ps1) -- reproduced from a pinned upstream tag,
# never committed, mirrors the dist/build entries above.
/backend/vendor
# PyInstaller --onedir output copied here before `tauri build` (~80 MB).
# Reproduced on every release build from `backend/dist/`, never edited by hand.
/src-tauri/resources

# Environment
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
.venv-*/
venv/

# OS
.DS_Store
Thumbs.db

# Build artifacts
*.egg-info/
.pytest_cache/
/.claude
/docs
/specs
/_bmad

# Local-only working documents
CLAUDE.md
train-audio-data/
